# Test Environment Configuration
NODE_ENV=test
PORT=3001

# Mock API Configuration (not used in tests but required for config)
EXCHANGE_RATE_API_KEY=test_key
EXCHANGE_RATE_API_URL=https://mock-api.test

# CORS Configuration
CORS_ORIGIN=*

